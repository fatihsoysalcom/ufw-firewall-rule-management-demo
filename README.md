# ufw-firewall-rule-management-demo
This script demonstrates how to manage firewall rules using UFW (Uncomplicated Firewall) on Linux. It covers setting default policies, adding allow and deny rules, listing active rules, and safely resetting UFW to its initial state. The script is designed for demonstration purposes and includes a full reset at the end to prevent unintended system c
